import { Typography, Grid, Container } from "@mui/material";

export default function Page() {
  return (
    <Grid container sx={{ color: "white", padding: 10}}>
      <Grid item>experience page</Grid>
    </Grid>
  );
}
